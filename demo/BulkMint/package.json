{
  "name": "bulkmint",
  "version": "1.0.0",
  "description": "BulkMint demo",
  "scripts": {
    "compile": "locklift build --config locklift.config.js",
    "deploy": "locklift run --config locklift.config.js --network devnet --script scripts/1-deploy-collection.js",
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@itgold/everscale-tip": "^0.8.1",
    "locklift": "git+https://github.com/broxus/ton-locklift.git"
  }
}
